import { FiArrowLeft, FiArrowUpRight } from "react-icons/fi";
import type { Project } from "../../types/content";
import { projects } from "../../data/projects";
import { useLanguage } from "../../controllers/LanguageController";
import { ProgressiveImage } from "../components/ProgressiveImage";
import { SmartLink } from "../components/SmartLink";
export function ProjectPage({ project }: { project: Project }) { const { locale, copy } = useLanguage(); const text = project.copy[locale]; const next = projects[(projects.indexOf(project)+1)%projects.length]; return <main id="main" className="case-page"><header className="case-hero"><SmartLink href={`/${locale}/#portfolio`} className="back-link"><FiArrowLeft/> {copy.case.back}</SmartLink><p className="section-label">{text.category} · {project.year}</p><h1>{project.name}</h1><p className="case-deck">{text.description}</p><ProgressiveImage src={project.image} alt={`${project.name} — ${text.title}`} className="case-cover" /></header><section className="case-meta"><div><span>{copy.case.role}</span><strong className="ai-generated-copy" data-content-origin="generated">Design &amp; Development</strong></div><div><span>{copy.case.type}</span><strong>{text.category}</strong></div><div><span>{copy.case.timeline}</span><strong>{project.year}</strong></div><div><span>{copy.case.status}</span><strong className="ai-generated-copy" data-content-origin="generated">Published</strong></div><div className="wide"><span>{copy.case.stack}</span><strong>{project.stack.join(" · ")}</strong></div></section><section className="case-narrative"><article><p className="section-label">01 / {copy.case.context}</p><h2>{copy.case.context}</h2><p className="ai-generated-copy" data-content-origin="generated">{text.context}</p></article><article><p className="section-label">02 / {copy.case.challenge}</p><h2>{copy.case.challenge}</h2><p className="ai-generated-copy" data-content-origin="generated">{text.context}</p></article><article><p className="section-label">03 / {copy.case.solution}</p><h2>{copy.case.solution}</h2><p className="ai-generated-copy" data-content-origin="generated">{text.solution}</p></article></section><section className="case-gallery"><p className="section-label">04 / {copy.case.gallery}</p>{project.gallery.map((image,index)=><ProgressiveImage key={image} src={image} alt={`${project.name}, ${copy.case.gallery.toLowerCase()} ${index+1}`} />)}</section><div className="case-actions">{project.href && <a className="button primary" href={project.href} target="_blank" rel="noreferrer">{copy.case.live}<FiArrowUpRight/></a>}</div><SmartLink href={`/${locale}/portfolio/${next.slug}`} className="next-project"><span>{copy.case.next}</span><strong>{next.name}</strong><FiArrowUpRight/></SmartLink></main>; }
